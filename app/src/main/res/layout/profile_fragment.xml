<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/light_gray"
    android:padding="16dp"
    android:gravity="center">

    <!-- Profile Picture with rounded corners -->
    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/profileImage"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/profile_placeholder"
        app:shapeAppearanceOverlay="@style/RoundedImage"
        android:layout_gravity="center" />

    <!-- Name and Age -->
    <TextView
        android:id="@+id/driverNameAge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Full Name : Oussama ET-TAJANI\n Age : 21"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textAlignment="center"
        android:layout_marginTop="16dp" />

    <!-- Information Text -->
    <TextView
        android:id="@+id/infoText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="If you want more information about the driver, please scan QR Code"
        android:textSize="16sp"
        android:textAlignment="center"
        android:layout_marginTop="24dp" />

    <!-- Get QR Code Button -->
    <Button
        android:id="@+id/getQRCodeButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Get QR Code"
        android:layout_marginTop="32dp" />

</LinearLayout>
